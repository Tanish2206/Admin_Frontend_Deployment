import UserMenu from "@/Components/userMenu/UserMenu";
import { Box } from "@chakra-ui/react";


const User: React.FC = () => {
  return (
    <Box p={4}>
  <UserMenu/>
  </Box>
  );
};

export default User;
